import type { NextPage } from "next";

import { Layout } from "components";

const Index: NextPage = () => {
    return (
        <Layout>
            <h1>Hello World!</h1>
        </Layout>
    );
};

export default Index;
