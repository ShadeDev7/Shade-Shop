type LayoutProps = {
    children: ReactNode;
};

type MenuAnchorProps = {
    href: string;
    className?: string;
    viewBox: string;
    paths: string[];
    children: ReactNode;
};

type AppProviderProps = {
    children: ReactNode;
};
