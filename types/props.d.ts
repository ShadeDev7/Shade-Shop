/// <reference types="react" />

type LayoutProps = {
    children: React.ReactNode;
};

type MenuAnchorProps = {
    href: string;
    className?: string;
    viewBox: string;
    paths: string[];
    children: ReactNode;
};
